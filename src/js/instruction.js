function Instruction (instruction_command, first_light_pos, last_light_pos) {

    this.instruction_command = instruction_command;
    this.first_light_pos = first_light_pos;
    this.last_light_pos = last_light_pos;

}